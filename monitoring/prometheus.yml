global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: xvet-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - xvet-backend:8081
        labels:
          application: xvet-backend
          env: prod
